#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Determines if digit is greater than 5, less than 6, or is 0
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char a;
for (a = 'a'; a <= 'z'; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
